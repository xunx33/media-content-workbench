# media-content-workbench 下一步完善计划

> 项目当前状态、已完成项、代码细节均以 git 历史为准，本文档只记录**后续要做的事**。

---

## 下一步计划（按优先级）

### 阶段 0：安全地基（已完成，仅备忘）
- [x] 修 XSS（全局 escapeHtml）
- [x] 禁 `/data` 静态暴露
- [x] 原子写入数据文件
- [x] 修硬编码 `/5` 进度 bug
- [x] 修复保存数据按钮崩溃（视频平台录入）
- [x] 导出重构为 4 表 + 周/月筛选
- [x] 导出标签居中对齐、顺序调整

### 阶段 1：PWA 桌面集成（推荐优先）
- [ ] **manifest.webmanifest** + 图标 + 注册逻辑
- [ ] **sw.js**：缓存壳（HTML/CSS/JS）+ `/api/data` 走网络优先离线降级
- [ ] 离线兜底 UX（断网 Toast + 内存数据展示）

### 阶段 2：体验精修
- [ ] 主题切换（浅/深/跟随系统），抽 CSS 变量到 `[data-theme]`
- [ ] 日历增强：点击未来日期直接新建任务
- [ ] 微交互升级（Tab 过渡、卡片磁吸、Toast 滑入）
- [ ] 空状态插画（SVG 替换 `[ + ]` 占位）

### 阶段 3：数据价值深化（按需）
- [ ] 平台横向对比（10 平台完成率/互动/收录率单页）
- [ ] 选题效果标签，复盘按标签聚合
- [ ] 周报/月报一键生成（Markdown）

### 阶段 4：架构升级（按需）
- [ ] 跨设备同步（CloudBase/Supabase 等）
- [ ] 自动化采集：对接平台 API 拉取播放量/AI 收录

---

## PWA 落地清单（阶段 1 详细）

> 当前项目是**纯静态前端 + Node 文件服务、零依赖零构建**，做 PWA 成本极低、收益高——每日高频工具装成桌面/手机 App 体验好，断网也能看历史数据。

### 1. manifest.webmanifest（应用身份证）

```json
{
  "name": "新媒体内容发布工作台",
  "short_name": "发布工作台",
  "description": "每日多平台内容发布与 AI 收录追踪",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#0f1115",
  "theme_color": "#0f1115",
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```
`index.html` 的 `<head>` 加 `<link rel="manifest" href="/manifest.webmanifest">` + `<meta name="theme-color" content="#0f1115">`。

### 2. 图标（2 张）
放 `icons/`：192×192 与 512×512。简单品牌色方块 + 文字"发"，与深色主题一致。

### 3. sw.js（Service Worker：缓存壳 + 离线）

```js
const CACHE = 'mcb-shell-v2';
const SHELL = ['/', '/index.html', '/css/style.css',
  '/js/app.js', '/js/store.js', '/js/ui.js', '/js/export.js',
  '/js/sample-migration.js', '/js/views/today.js', '/js/views/content.js',
  '/js/views/data.js', '/js/views/calendar.js', '/js/views/overview.js',
  '/js/views/table-parser.js'];

self.addEventListener('install', e => {
  e.waitUntil(caches.open(CACHE).then(c => c.addAll(SHELL)));
  self.skipWaiting();
});
self.addEventListener('activate', e => {
  e.waitUntil(caches.keys().then(keys =>
    Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k)))));
  self.clients.claim();
});
self.addEventListener('fetch', e => {
  const url = new URL(e.request.url);
  if (url.pathname.startsWith('/api/')) {
    e.respondWith(fetch(e.request).catch(() =>
      new Response('{"offline":true}', {headers:{'Content-Type':'application/json'}})));
  } else {
    e.respondWith(caches.match(e.request).then(r => r || fetch(e.request)));
  }
});
```

### 4. 注册逻辑
`app.js` 末尾或 `<body>` 末尾：
```js
if ('serviceWorker' in navigator) {
  window.addEventListener('load', () => navigator.serviceWorker.register('/sw.js'));
}
```

### 5. 离线兜底 UX
`/api/data` 拉取失败时，前端内存中已加载的 `contents` 仍在，可正常显示当次会话数据；断网弹"离线模式，显示本地缓存"Toast。

### 6. 验收标准
- [ ] 桌面 Chrome/Edge 地址栏出现"安装"图标，可装到桌面
- [ ] 手机"添加到主屏幕"后全屏打开、无地址栏
- [ ] 断网后重新打开，壳（界面）正常显示，数据接口降级提示
- [ ] 改前端代码必须升 `CACHE` 版本号，否则用户拿旧壳

### 7. 注意
- 必须先完成"禁 `/data` 静态暴露"（阶段 0 已做），否则装成 App 数据仍裸奔。
- 保持零依赖，不为 PWA 引入 webpack/vite。
