# media-content-workbench 项目审阅与完善计划

> 审阅时间：2026-08-03 · 审阅人：高级开发工程师  
> 仓库：xunx33/media-content-workbench · 本地路径：`media-content-workbench/`

---

## 一、项目现状速览

| 维度       | 现状                                                           |
| -------- | ------------------------------------------------------------ |
| **定位**   | 新媒体内容发布工作台：4 短视频 + 5 文书平台，每日各 1 条发布管理 + AI 收录追踪              |
| **技术栈**  | 纯原生 HTML/CSS/JS，零依赖、零构建                                      |
| **代码体量** | 单文件 `index.html`，1291 行（CSS ~500 行 + HTML ~30 行 + JS ~760 行） |
| **数据存储** | 浏览器 localStorage（4 张表：tasks / contents / stats / aiStats）    |
| **目标设备** | 移动端优先（PWA 风格 meta，已做 iOS 安全区适配）                              |
| **主题**   | 仅深色主题                                                        |

**9 个平台**：抖音、快手、小红书、视频号（视频）；百家号、公众号、知乎、企鹅号、搜狐号（文书）  
**6 个 AI 引擎**：DeepSeek、豆包、千问、文心、元宝、纳米

---

## 二、功能审阅（5 个 Tab）

| Tab       | 已实现能力                          | 评价                       |
| --------- | ------------------------------ | ------------------------ |
| **今天要处理** | 9 平台日清单、勾选完成、本日概览统计            | 核心闭环完整，够用                |
| **发布日历**  | 月历、圆点状态、当日明细、逾期标记              | 视觉清晰，但只能查看不能在日历上直接新增未来任务 |
| **平台管理**  | 每平台今日/本月进度、快捷完成按钮              | 实用，缺少趋势和环比               |
| **内容登记**  | 增删改查、筛选（全部/今日/视频/文书）、链接跳转      | 够用，但缺少搜索、缺少内容与任务的关联入口    |
| **数据复盘**  | 视频互动数据录入+柱状图；文书 AI 收录勾选+分布图+表格 | 双视角设计很好，图表偏基础            |

**附加功能**：导出/导入 JSON、清空、示例重置、30 条备份提醒、Toast、模态框、FAB。

---

## 三、前端设计审阅

### 优点（做得不错的地方）

- **色彩系统规范**：CSS 变量统一管理，视频用橙红渐变、文书用蓝紫渐变，语义一致
- **移动端适配扎实**：768px / 480px 两档断点，`env(safe-area-inset)` 适配刘海屏，输入框 `font-size:16px` 防 iOS 缩放
- **状态语义清晰**：完成（绿）/待办（黄）/逾期（红）颜色编码到位
- **交互模式完整**：Toast 反馈、模态框、FAB、粘性头部、横向滚动 Tab

### 问题与差距（按严重度排序）

| #  | 问题                    | 严重度 | 说明                                              |
| -- | --------------------- | --- | ----------------------------------------------- |
| 1  | **无浅色主题/主题切换**        | 中   | 只有深色，长时间使用疲劳；不符合多场景使用                           |
| 2  | **数据全在 localStorage** | 中   | 清缓存即丢失，无法跨设备同步，5~10MB 上限风险                      |
| 3  | **XSS 风险**            | 中   | `innerHTML` 直接拼接用户输入（标题、链接），未做转义                |
| 4  | **内容与任务未打通**          | 中   | Task 有 `contentId` 字段但新增模态从不写入，关联逻辑空挂           |
| 5  | **无 PWA 安装能力**        | 低   | 有 meta 但无 manifest.json、无 Service Worker，无法装到桌面 |
| 6  | **原生 confirm() 弹窗**   | 低   | 风格割裂，体验不一致                                      |
| 7  | **无搜索**               | 低   | 内容多了之后无法快速定位                                    |
| 8  | **无障碍缺失**             | 低   | 大量 `onclick` 挂在 `div` 上，无 ARIA、无键盘导航            |
| 9  | **动效偏弱**              | 低   | 无 Tab 切换过渡、无磁吸/微交互，离"高级感"有距离                    |
| 10 | **无数据趋势图**            | 低   | 只有当月汇总柱状图，看不到时间趋势                               |

---

## 四、下一步完善计划（分阶段、可落地）

### 阶段 0：地基修复（1~2 天，建议先做）

> 目标：堵住数据与安全漏洞，不新增功能。

- [ ] **0.1 修复 XSS**：封装 `esc()` 转义函数，所有 innerHTML 拼接的用户内容（title/topic/url）统一过一遍
- [ ] **0.2 打通内容↔任务关联**：新增内容时可选关联当日该平台任务，写入 `task.contentId`；"今天要处理"和"日历明细"自动显示关联标题
- [ ] **0.3 localStorage 容量看护**：启动时检测用量，超 80% 提醒导出；任务表按月归档（只保留近 3 个月明细）
- [ ] **0.4 统一确认弹窗**：用现有模态框替换原生 `confirm()`，风格一致

### 阶段 1：体验精修（2~3 天）

> 目标：让它从"能用"变"好用、好看"。

- [ ] **1.1 主题切换（浅色/深色/跟随系统）**：抽出 CSS 变量到 `[data-theme]` 作用域，加头部切换按钮，记忆偏好
- [ ] **1.2 PWA 化**：补 `manifest.json` + 图标 + 简易 Service Worker（缓存壳），可"添加到主屏幕"离线打开
- [ ] **1.3 内容搜索**：内容 Tab 顶部加搜索框，按标题/选题/平台实时过滤
- [ ] **1.4 日历增强**：点击未来日期可直接为该日某平台新建/取消任务；圆点改为可悬停预览
- [ ] **1.5 微交互升级**：Tab 切换淡入、卡片磁吸悬停、完成勾选有粒子反馈、Toast 滑入动效
- [ ] **1.6 空状态插画**：用纯 CSS/SVG 替换 `[ + ]` 文字占位

### 阶段 2：数据价值深化（2~3 天）

> 目标：让复盘真正能指导创作决策。

- [ ] **2.1 趋势折线图**：数据 Tab 增加近 30 天播放量/AI 收录率折线（轻量自绘 SVG，免引库）
- [ ] **2.2 平台横向对比**：单页对比 9 平台的完成率、互动、收录率，一目优劣
- [ ] **2.3 选题效果标签**：内容登记加"选题标签"维度，复盘可按标签聚合看哪类选题更易被 AI 收录
- [ ] **2.4 周报/月报一键生成**：导出 Markdown 格式周报，自动汇总完成率、TOP 内容、收录变化

### 阶段 3：架构升级（按需，3~5 天）

> 目标：当数据量或协作需求增长时再做。

- [ ] **3.1 拆分单文件**：HTML/CSS/JS 分离，或迁到 Vite + 轻量框架（Vue/Lit），提升可维护性
- [ ] **3.2 后端化（可选）**：接入轻量后端（如 Supabase / 腾讯云 CloudBase）实现跨设备同步与登录
- [ ] **3.3 自动化采集（高阶）**：对接平台开放 API 或浏览器自动化，自动拉取播放量、检测 AI 收录，免去手工录入

---

## 五、建议的执行顺序

```
阶段0 地基修复  →  阶段1 体验精修  →  阶段2 数据深化  →  阶段3 架构升级(按需)
   (必做)            (强烈推荐)          (有精力就做)        (数据/协作驱动)
```

**如果只挑 3 件事先做**，我建议：

1. **阶段 0.1 修 XSS + 0.2 打通内容关联** —— 让数据模型正确
2. **阶段 1.1 主题切换** —— 立竿见影提升观感
3. **阶段 1.2 PWA 化** —— 让你能把它装到手机桌面当 App 用

---

## 六、技术选型备注（给非技术背景的 Jeff）

- **PWA**：就是让网页能像 App 一样装到手机/电脑桌面，离线也能打开。你的工作台是每日高频工具，装成 App 体验好很多。
- **Service Worker**：PWA 的"后台管家"，负责缓存，断网时还能显示内容。
- **XSS**：恶意内容注入漏洞。你的标题/链接目前直接塞进页面，如果有人导入一份恶意 JSON，可能被执行。修起来很简单（加个转义）。
- **localStorage**：浏览器自带的本地小数据库，但只在当前浏览器、清缓存就没了，也无法手机电脑同步。

需要我从哪一步开始动手实现，告诉我即可。
