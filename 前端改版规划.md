# 前端改版规划 · 严格对齐 preview-warm-studio.html

> **基准**：所有视觉决策以 `preview-warm-studio.html` 的实际 CSS 为准（暖炭黑底 + 暖金主色 + 宋体标题 + 等宽数字 + 实色面板 + 去毛玻璃/渐变/发光）。
> **范围**：只换皮肤，不重构结构、不删功能、不改业务逻辑。
> **策略**：分两刀。第一刀纯 CSS（零功能风险）；第二刀轻改 JS 渲染字符串（让首字大字平台名覆盖 today/content 两页）。

---

## 一、新设计令牌（直接取自 preview 的 `:root`，仅变量名对齐真实项目）

```css
:root{
  /* 底色：冷藏蓝 → 暖炭黑 */
  --bg:#14110d;  --bg-2:#1a150f;
  --surface:#241d16;  --surface-2:#2c231a;
  --border:rgba(245,222,179,0.13);  --border-strong:rgba(245,222,179,0.26);
  --text:#f3ead9;  --text2:#c9b89c;  --text3:#8f7f68;

  /* 主操作色：蓝紫渐变 → 单一暖金（不再双色渐变） */
  --accent:#d9a441;   --accent-2:#d9a441;   /* 与 accent 同色，原渐变自动变单色 */
  --accent-ink:#1a150f;   --accent-soft:rgba(217,164,65,0.16);
  --link-blue:#e8c07a;    /* 正文链接改为暖金，替代冷蓝 #7da7ff */

  /* 语义色：视频橙 / 文书梅紫，按 preview 调暖（保留业务区分） */
  --video-orange:#ef9a4d;  --video-orange-light:#f3b87a;
  --article-purple:#b98ab0; --article-purple-light:#d3b3cb;
  --green:#8fbf7a;  --red:#d9745f;  --yellow:#d9a441;

  /* 字体（系统字体，离线可用，PWA 友好） */
  --serif:"Songti SC","STSong","SimSun","宋体",serif;
  --sans:"PingFang SC","Microsoft YaHei","Noto Sans SC",system-ui,sans-serif;
  --mono:"JetBrains Mono",ui-monospace,"SF Mono",Menlo,Consolas,monospace;

  /* 圆角 / 阴影（去发光） */
  --radius:14px; --radius-sm:9px; --radius-xs:7px;
  --shadow:0 6px 22px rgba(0,0,0,0.45);  --shadow-sm:0 2px 10px rgba(0,0,0,0.3);
  --glow-accent:none;   /* 原发光变量停用 */
}
```

**字体取舍**：不引入 Google Fonts（保 PWA 离线）。标题用系统宋体回退（Mac `Songti SC` / Win `宋体`），数字用系统等宽（`SF Mono`/`Consolas`）。代价：不同系统宋体观感略差异；把字体打进缓存属进阶优化，暂不做。

---

## 二、组件级改造清单（真实选择器 → preview 目标）

| # | 真实组件 | 真实选择器 | preview 目标样式 | 改法 |
|---|---|---|---|---|
| 1 | 顶栏 | `.topbar` (L59-62 有 `backdrop-filter:blur(20px)`) | `.topbar` 实色 `var(--surface)` + 边框 + 阴影 | 删 `backdrop-filter`，背景改 `var(--surface)` |
| 2 | 导航 TAB | `.nav-tab` / `.nav-tab.active::after` (L96-118) | `.tab` 胶囊：激活态 `background:var(--accent);color:var(--accent-ink);border-radius:20px`，**无渐变无发光** | 删 `.nav-tab.active::after` 渐变+发光，`.nav-tab.active` 改实色暖金胶囊 |
| 3 | 子 TAB | `.sub-tab` / `.sub-tab.active` (L893-902) | 同 nav-tab 风格 | 同步：激活实色暖金，去渐变 |
| 4 | 卡片 | `.card` (L178-184 有 `backdrop-filter:blur(14px)`) | `.card` 实色 `var(--surface)` + 细边 | 删 `backdrop-filter`，背景改实色 |
| 5 | 卡片标题 | `.card-title` | `.card-title` 宋体 + `::before` 3px 暖金竖条 | 挂 `var(--serif)`；补 `::before` 暖金竖条（如已有可改色） |
| 6 | 统计卡 | `.stat-card` / `.stat-value` (L597-612) | `.stat .v` 等宽数字 `font-variant-numeric:tabular-nums` | `.stat-value` 挂 `var(--mono)` + tabular-nums |
| 7 | 平台行图标 | `.platform-row-icon.video/.article` (L430-431 渐变) | `.pico` 实色暖橙/梅紫，36px 圆角方形，深字 | 渐变→实色 `--video-orange` / `--article-purple` |
| 8 | 平台标签 | `.platform-tag.video/.article` (L271-272) | `.ptag` 边框标签（半透明底+同色边框） | 去渐变，改半透明底+同色字+同色细边 |
| 9 | 内容列表项 | `.content-item` + `.video/.article` 左色条 (L508-516) | `.ci` 左侧 3px 色条区分视频/文书 | 左色条颜色改用暖 `--video-orange` / `--article-purple` |
| 10 | 按钮（主/录入/保存/危险/FAB） | `.btn-edit/.btn-data-entry/.btn-save/.btn-danger/.fab/.toolbar .btn-primary` 等约 12 处 | `.btn` 实色、无渐变、无发光 | 渐变→实色（`--accent` / `#8fbf7a` / `#d9745f`），hover 发光阴影→普通位移阴影 |
| 11 | 柱状图 | `.bar.video/.bar.article` (L654-655 渐变+发光) | `.bar` 实色 | 渐变+发光→实色 `--video-orange` / `--article-purple` |
| 12 | 图表图例 | `.chart-legend .legend-dot.video/.article` (L678-679) | 实色 | 同 bar |
| 13 | 标题渐变文字 | `.header h1` (L73-77) + 另一处 `background-clip:text` (L608-609) | `.brand h1` 宋体实色 | 删 `background-clip:text` 渐变，改 `var(--text)` 或 `var(--accent)` 实色 + 挂 `var(--serif)` |
| 14 | 模态/弹窗 | `.modal` 类 (L235-236 `backdrop-filter`) | 实色面板 | 删 `backdrop-filter`，背景改实色 |
| 15 | Tooltip | (L702 `backdrop-filter`) | 实色 | 删 `backdrop-filter` |
| 16 | 搜索框/输入框 focus 环 | `.search-box:focus-within` / `select:focus` (L727,779,941) | 实色描边 | 发光环 `0 0 0 3px var(--accent-soft)` → 普通描边 `border-color:var(--accent)`（保留 soft 但去发光） |
| 17 | 徽章 | `.badge` (L825) | `.badge` 暖金半透明底 | 改 `background:var(--accent-soft);color:var(--accent)` |
| 18 | 状态文字 | `.status-done/.status-pending/.status-overdue` (L274-276) | `.dot` 绿/灰 | `.status-done`→`--green`；`.status-pending`→`--text3`（灰，对齐 preview pending 灰点）；`.status-overdue`→`--red` |
| 19 | 日历选中/进度 | `.calendar-day.selected` 等 | 实色 | 发光环→实色描边 |
| 20 | 全局字体 | `body` (L40) | sans 正文 | `body` 字体栈改 `var(--sans)`；标题类（`.header h1`/`.card-title`/`.calendar-header` 等）挂 `var(--serif)` |
| 21 | 颗粒纹理（可选） | 无 | `body::before` 极淡 noise (opacity .035) | 新增，增加手工感、去塑料感（可选，不强求） |

---

## 三、JS 内联样式（需顺手改，避免漏色）

- `overview.js:63` 与 `:80` 进度条内联 `linear-gradient(90deg,var(--video-orange),var(--video-orange-light))` → 改实色 `var(--video-orange)`；文书同理改 `--article-purple`。
- `content.js` 内容列表项：在标题/平台行复用首字色块（见第四·第二刀）。

---

## 四、执行顺序（两刀）

**第一刀（纯 CSS，零功能风险）**
1. 替换 `:root` 令牌（第一章）→ 2. 去毛玻璃（#1/#4/#14/#15）→ 3. 去渐变文字（#13）→ 4. 按钮/标签/TAB/图表去渐变+发光（#2/#3/#10/#11/#12）→ 5. 平台图标/标签/内容左色条换暖实色（#7/#8/#9）→ 6. 挂字体（#20）+ 徽章/状态/描边（#17/#18/#16）→ 7. 升 `?v=` + `CACHE`
8. `node tests/run.js` 确认无回归 → 你 `Ctrl+Shift+R` 强刷看效果

**第二刀（轻 JS，首字大字平台名全站统一）**
9. `today.js` 今日待办平台行 + `content.js` 内容列表项：复用现有 `PLATFORM_SHORT[p]` + `.platform-row-icon` 样式（暖色版），在平台名前加首字色块 → 10. 升 `?v=` + `CACHE` → 11. 回归测试

> 第二刀若暂不想动 JS，可只做第一刀——首字块在**概览页（overview.js）本来就用上了**，today/content 不加不影响功能，只是全站未统一。

---

## 五、防缓存坑（必做，否则旧壳卡死）

- `index.html` 所有 `css/style.css?v=` → `20260813d`
- `sw.js` 的 `const CACHE` → `mcb-shell-v6`
- 旧规矩：改前端必须 **?v= 与 SW CACHE 同时升**，你之前踩过双层卡死坑。

---

## 六、验证

- `node tests/run.js`：确认 JS 渲染与导入导出对称未被破坏（9 项全过）。
- 视觉校验（强刷后）：① TAB 无发光、激活为暖金胶囊；② 平台名有首字色块（概览页本就有）；③ 整屏暖炭黑+暖金；④ 标题宋体、统计数字等宽；⑤ 按钮/卡片/图表无渐变无毛玻璃。
- 离线校验：断网打开应用，界面与字体正常（无白屏、无字体闪烁）。

---

## 七、不在本次范围

- 不重构 HTML 结构、不增删页面、不改业务逻辑。
- 不引入在线字体（PWA 离线考量）。
- 不重做浅色版。
- 导出报表（HTML 报表固定样式）如需同步暖色另行规划，不在本刀。

---
*规划文档，未改动任何项目文件。确认后回复"开始第一刀"或"两刀都做"，即按此执行。*
