# 媒体工作台 · 后端接入部署指南

> 生成时间：2026-08-06 · 方案：腾讯云 CloudBase（方案 A）
> 用途：**部署后随时随地访问 + 云端存储 + 分享给特定的人只读查看**
> 状态：**代码已完成，等你按本指南完成部署**

---

## 一、当前已完成（代码层）

### 1.1 新增文件

```
media-content-workbench/
├── js/
│   ├── cloud.js                  ★ 云核心：登录/注册/数据同步/分享管理/访客模式
│   ├── cloud-ui.js               ★ 云状态条 + 登录弹窗 + 分享管理面板
│   └── cloud-config.example.js   ★ 环境配置模板（部署时复制为 cloud-config.js）
├── cloudfunctions/               ★ 4 个云函数（登录/注册/分享）
│   ├── login/index.js            账号密码登录，签发凭证
│   ├── register/index.js         首次注册（第一人为管理员）
│   ├── share_manage/index.js     生成/关闭分享令牌
│   └── share_get/index.js        访客只读拉取数据
└── 后端接入方案.md                选型与架构文档
```

### 1.2 修改文件

| 文件 | 改动 |
|------|------|
| `js/store.js` | `saveData` 登录后自动触发云同步（防抖 1.5s），**业务代码零改动** |
| `index.html` | 引入 cloud-config + CloudBase SDK CDN + cloud.js/cloud-ui.js，新增云状态条容器 |
| `js/app.js` | 访客模式/正常模式分流启动 |
| `css/style.css` | 云状态条样式 + 访客只读模式隐藏编辑按钮 |
| `.gitignore` | 排除 `js/cloud-config.js`（含敏感 envId，勿提交） |

### 1.3 设计要点

- **无侵入**：`loadData/saveData` 签名不变，现有 2000 行业务代码不用动
- **本地兜底**：未登录/未部署时走 localStorage，完全离线可用
- **访客只读**：`?view=令牌` 链接 → 只读模式，隐藏编辑/删除/导出按钮

---

## 二、部署步骤（你需要做的）

### 第 1 步：创建 CloudBase 环境（10 分钟）

1. 打开 [腾讯云 CloudBase 控制台](https://console.cloud.tencent.com/tcb)
2. 用你已实名的腾讯云账号登录
3. 点「新建环境」→ 按提示创建（选免费套餐即可）
4. 创建后记录**环境 ID**（形如 `xxx-1a2b3c4d5e`，在环境概览页）

### 第 2 步：填写环境配置（2 分钟）

1. 在 `js/` 目录下**复制** `cloud-config.example.js` → 重命名为 `cloud-config.js`
2. 打开 `cloud-config.js`，把 `your-env-id` 替换为你的环境 ID：

```js
const CLOUD_CONFIG = {
  envId: '你的环境ID',  // ← 改成你的
  region: 'ap-shanghai' // 如环境在其他地域则改
};
```

> ⚠️ `cloud-config.js` 已在 .gitignore 中，不会提交到仓库，放心填。

### 第 3 步：部署云函数（15 分钟）

CloudBase 控制台 → 「云函数」→ 逐个创建以下 4 个函数：

| 函数名 | 目录 | 说明 |
|--------|------|------|
| `login` | `cloudfunctions/login/` | 账号密码登录 |
| `register` | `cloudfunctions/register/` | 首次注册（管理员） |
| `share_manage` | `cloudfunctions/share_manage/` | 分享令牌管理 |
| `share_get` | `cloudfunctions/share_get/` | 访客只读数据 |

每个函数：
1. 「新建云函数」→ 名称填对应名字
2. 上传方式选「本地上传文件夹」→ 选择对应目录（含 index.js 和 package.json）
3. 运行时选 **Nodejs 16.13 或以上**
4. 创建后点「测试」确认部署成功

### 第 4 步：创建数据库集合（5 分钟）

CloudBase 控制台 → 「数据库」→ 新建 3 个集合：

| 集合名 | 用途 |
|--------|------|
| `users` | 管理员账号（注册时自动写入） |
| `workbench_sync` | 你的 5 张表数据（同步用） |
| `share_config` | 分享令牌配置 |

权限设置：**全部设为「所有用户不可读写」**（数据只通过云函数访问，安全）。

### 第 5 步：静态托管部署（10 分钟）

1. CloudBase 控制台 → 「静态网站托管」→ 开通
2. 上传整个 `media-content-workbench/` 目录（含 index.html、css、js、cloudfunctions 可选）
3. 部署完成后获得网址：`https://xxx.tcloudbaseapp.com`
4. 手机/电脑打开该网址即可访问

> 注意：之后每次改代码，重新上传 index.html + js/ + css/ 即可。

### 第 6 步：注册管理员账号（3 分钟）

1. 打开部署后的网址
2. 顶部云状态条显示「未登录」→ 点「注册」
3. 设置账号密码（密码 ≥6 位）→ 注册成功自动登录
4. 状态条变为「☁️ 已同步 · 你的账号」

> 第一个注册的人就是管理员，可管理分享。之后不需要再注册。

### 第 7 步：验证（5 分钟）

| 验证项 | 预期 |
|--------|------|
| 页面顶部状态条 | 「☁️ 已同步 · 账号」 |
| 登记一条内容 | 状态条短暂变化后恢复（已自动同步） |
| 手机打开网址 | 登录后能看到相同数据（跨设备同步） |
| 断网打开 | 本地数据仍可用（离线兜底） |

---

## 三、分享模式使用（发给特定的人看）

### 主账号（你）操作

1. 登录后点状态条上的「分享管理」
2. 点「开启分享」→ 自动生成只读链接
3. 点「复制链接」→ 发给想分享的人

```
分享链接示例：https://xxx.tcloudbaseapp.com/?view=7f3a9c8b...
```

### 访客（收到链接的人）

- 打开链接 → 显示「🔗 访客分享模式 · 只读查看」
- 可查看所有内容登记/数据/复盘/图表
- **不能**编辑/删除/导出（按钮已隐藏）
- 无需注册登录

### 收回分享

- 主账号 → 分享管理 → 「关闭分享」→ 所有旧链接立即失效

---

## 四、安全说明

- 分享令牌为 32 位随机串，不可猜测
- 数据只通过云函数访问（数据库对外完全关闭）
- 访客只读：即使拿到令牌也只能读，不能写
- 换令牌即可让旧链接失效

---

## 五、常见问题（FAQ）

**Q1：部署后打开页面显示「分享链接无效或已关闭」？**
- 检查 URL 是否带 `?view=令牌` 且令牌与 `share_config` 一致
- 检查分享是否被关闭

**Q2：登录报错「账号不存在」？**
- 先点「注册」创建管理员账号，再登录

**Q3：数据没有同步？**
- 检查登录状态（状态条是否显示「已同步」）
- 检查云函数 login 是否部署成功
- 检查数据库集合 `workbench_sync` 是否存在

**Q4：改了代码怎么更新部署？**
- 重新上传 index.html + js/ + css/ 到静态托管即可

**Q5：cloud-config.js 要不要提交 git？**
- **不要**。它含环境 ID，已被 .gitignore 排除。部署时手动创建。

---

## 六、后续方向（可选）

部署稳定后，可继续：
- **PWA 化**：manifest + Service Worker，装到手机桌面当 App
- **数据备份**：CloudBase 自带每日备份，也可保留本地导出
- **多管理员**：如需多人协作，扩展 register 云函数支持多账号
