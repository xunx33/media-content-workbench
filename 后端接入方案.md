# 媒体工作台 · 后端数据存储接入方案

> 规划时间：2026-08-06 · 规划人：高级开发工程师
> 目标：**部署后随时随地访问 + 云端存储所有发布内容信息**

---

## 〇、为什么现在是好时机

你的项目是**纯静态前端**（HTML/CSS/JS 零依赖、零构建），数据层高度集中：

```js
// 5 张表，全部走这两个函数（store.js 第 12-19 行）
function loadData(key) { return JSON.parse(localStorage.getItem(STORAGE_KEY + key)) || []; }
function saveData(key, val) { localStorage.setItem(STORAGE_KEY + key, JSON.stringify(val)); }

tasks / contents / stats / aiStats / reviews
```

**关键优势**：后端接入只需把这两个函数改造成「DAO 数据层」——本地缓存 + 云端同步 + 离线队列，**其余 2000 行业务代码一行不用动**。改造点非常集中，风险可控。

**现状痛点**：数据只在当前浏览器 localStorage，清缓存即丢、无法跨设备、电脑手机不互通、无法分享。

---

## 一、方案总览（两个推荐方案）

| 维度 | 方案 A：腾讯云 CloudBase 云开发 | 方案 B：Vercel + Supabase |
|------|-------------------------------|--------------------------|
| **性质** | 腾讯云一体化"云开发"（静态托管+云数据库+登录） | 全球主流方案（Vercel 托管 + Supabase 开源 BaaS） |
| **部署位置** | 国内（广州/上海等） | 国外（Vercel 全球 CDN） |
| **访问速度（国内）** | 🟢 快 | 🟡 一般（可接受，偶有波动） |
| **免费额度** | 静态托管免费 + 云数据库免费额度（约 1GB 存储/50k 次调用/月） | Vercel 免费 + Supabase 免费（500MB 数据库） |
| **成本（个人用）** | 约 0 元/月 | 约 0 元/月 |
| **登录方式** | 微信登录 / 匿名登录（最省事） | 邮箱密码 / Google / GitHub |
| **改造量** | 小（SDK 前端直连） | 中（需要后端函数做代理） |
| **数据所有权** | 腾讯云（国内合规） | 自己完全掌控（开源 Postgres） |
| **适合人群** | 追求省事、国内体验优先 | 追求开放性、想学主流技术栈 |

---

## 二、方案 A：腾讯云 CloudBase 云开发（推荐 ⭐）

### 2.1 架构

```
┌─────────────────────────────────────────┐
│  浏览器（PC / 手机 / 平板）                │
│  index.html + css/ + js/                │
│  └── store.js 改造为 DAO 层               │
│      本地 localStorage（离线缓存）          │
│      CloudBase SDK（云端同步）             │
└──────────────┬──────────────────────────┘
               │ HTTPS
┌──────────────▼──────────────────────────┐
│  腾讯云 CloudBase                         │
│  ① 静态托管：你的网页直接部署，全球可访问      │
│  ② 云数据库（文档型，类似 JSON）：5 张集合     │
│     tasks / contents / stats / aiStats /  │
│     reviews                               │
│  ③ 匿名登录：免注册，首次访问自动分配身份       │
│  ④ 云函数（可选）：数据校验/统计聚合           │
└──────────────────────────────────────────┘
```

### 2.2 核心改造点（1 处）

```js
// store.js — 把这两个函数改成"本地+云端"双写
async function saveData(key, val) {
  localStorage.setItem(STORAGE_KEY + key, JSON.stringify(val));   // 先写本地（秒响应）
  await db.collection(key).doc('mine').update({ data: val });     // 再同步云端
}
async function loadData(key) {
  const local = localStorage.getItem(STORAGE_KEY + key);          // 先读本地（离线可用）
  if (local) return JSON.parse(local);
  const cloud = await db.collection(key).doc('mine').get();       // 首次拉云端
  return cloud.data || [];
}
```

> 说明：数据按「用户」隔离，每人的内容只属于自己（`_openid` 字段由平台自动注入，天然多用户安全）。你的数据结构是 JSON 数组，CloudBase 文档型数据库完美匹配。

### 2.3 部署（静态托管，5 分钟）

```
CloudBase 控制台 → 静态网站托管 → 上传 media-content-workbench 目录 → 获得 https://xxx.tcloudbaseapp.com 网址
```

手机/电脑打开网址即用，数据自动同步。**也可以绑定自己的域名**。

### 2.4 数据迁移（现有 localStorage 数据）

已有 JSON 导出/导入功能（v3）：
1. 部署前在旧浏览器「导出JSON」备份
2. 部署后首次打开 → 用「导入恢复」把数据上传云端
3. 之后所有设备自动同步

### 2.5 成本与限制

| 项目 | 免费额度 | 个人使用量预估 |
|------|---------|---------------|
| 静态托管 | 免费 | ✅ 够用 |
| 云数据库 | 1GB 存储 / 50k 读 / 50k 写每月 | ✅ 你的 5 张表每天几十次操作，绰绰有余 |
| 登录 | 匿名登录免费 | ✅ |

**优点**：国内访问快、微信生态、登录最简单、文档全中文、改造量最小
**缺点**：绑定腾讯云（迁移性一般）、免费额度有上限（个人用远不会触及）

---

## 三、方案 B：Vercel + Supabase（开放性方案）

### 3.1 架构

```
┌─────────────────────────────────────────┐
│  浏览器                                    │
│  └── store.js 改造为 DAO 层（同上）          │
└──────────────┬──────────────────────────┘
               │ HTTPS
┌──────────────▼──────────────────────────┐
│  Vercel（静态托管，全球 CDN）               │
│  你的网页部署：xxx.vercel.app 全球可访问     │
└──────────────┬──────────────────────────┘
               │ REST/PostgREST
┌──────────────▼──────────────────────────┐
│  Supabase（开源 BaaS，Postgres 数据库）     │
│  ① 5 张表（tasks/contents/stats/aiStats/  │
│     reviews），每行带 user_id 隔离          │
│  ② Auth 登录（邮箱/GitHub/Google）          │
│  ③ Row Level Security 行级安全             │
└──────────────────────────────────────────┘
```

### 3.2 与方案 A 的差异

- **部署**：Vercel（GitHub 推代码自动部署，比 CloudBase 静态托管更"现代化"）
- **数据库**：Supabase 是**开源 Postgres**——SQL 数据库，可查询聚合（如"近30天播放量趋势"可以一条 SQL 算出来），而 CloudBase 是文档型
- **登录**：邮箱密码为主（比匿名登录多一步，但更"正规"）
- **访问速度**：Vercel 国内访问一般，**数据库 Supabase 在国外**，写入有延迟（个人使用可接受，偶尔慢）

### 3.3 核心改造点

```js
// 与方案 A 类似，但走 REST API + 行级安全
const { data, error } = await supabase
  .from('contents')
  .upsert({ user_id: user.id, ...content }, { onConflict: 'user_id,id' });
```

### 3.4 成本

| 项目 | 免费额度 |
|------|---------|
| Vercel | 100GB 带宽/月（个人用不到 1%） |
| Supabase | 500MB 数据库 / 5 万 MAU |

**优点**：开源可控、Postgres 强大（数据分析能力强）、主流技术栈学习价值高、Vercel 部署体验一流
**缺点**：国内访问速度一般、登录流程略繁琐、改造量比 A 多约 1 天、资料多为英文

---

## 四、两个方案怎么选

| 你的关注点 | 选哪个 |
|-----------|--------|
| 国内随时随地快 | **方案 A** ⭐ |
| 省事、登录简单 | **方案 A** ⭐ |
| 微信/手机为主 | **方案 A** ⭐ |
| 想学主流技术栈（SQL/GitHub 部署） | 方案 B |
| 数据完全自控、长期开源 | 方案 B |

**我的建议：先做方案 A（CloudBase）**——你人在国内、主要用手机访问、追求省事，CloudBase 的匿名登录 + 国内 CDN + 中文文档体验最好，改造量最小（1 天）。等数据量大到需要复杂分析时，再考虑迁到 Postgres 类方案（数据结构不变，DAO 层替换即可）。

---

## 五、实施计划（方案 A，约 3~4 天）

### ✅ 已确认的决策（2026-08-06）

| 项 | 决策 | 说明 |
|----|------|------|
| 腾讯云账号 | 已实名 | 可直接开通 |
| 登录方式 | **账号+密码（自定义登录）** | CloudBase 云函数签发 ticket，零短信成本，免记验证码 |
| 分享权限 | 只读查看 | 应用层令牌，访客无需注册 |
| 部署域名 | CloudBase 免费域名 | `xxx.tcloudbaseapp.com`，无需备案 |
| 现有数据 | 可丢 | 迁移简化，不追求完整 |
| 设备 | 手机+电脑都要 | 移动端优先适配 |
| 本地模式 | **保留** | 未登录/断网用 localStorage 兜底 |
| 实施节奏 | **先写代码，后部署** | 本地可验证逻辑 |

### 阶段 0~4：后端接入（2~3 天）

| 阶段 | 事项 | 耗时 |
|------|------|------|
| 0 | 注册腾讯云 → 开通 CloudBase → 创建环境 | 0.5h |
| 1 | store.js 改造成 DAO（本地+云端双写、离线队列、首次拉取） | 0.5 天 |
| 2 | 账号密码登录接入 + 登录状态 UI（顶部显示"已同步"） | 0.5 天 |
| 3 | 静态托管部署 + 绑定域名 | 2h |
| 4 | 数据迁移（导出→导入云端）+ 多设备验证 | 2h |
| 5 | 同步状态提示（在线/离线 Toast）+ 冲突处理（以后端为准） | 0.5 天 |

### 阶段 5：分享模式（应用层，约 1 天）⭐ 新增

> 需求：把页面分享给特定的人，对方随时随地只读查看数据，无需注册。

| 步骤 | 事项 |
|------|------|
| 5.1 | 设置页新增「分享管理」：生成/关闭分享开关 |
| 5.2 | 生成随机分享令牌（如 `7f3a9c...`），存储到云端配置集合 |
| 5.3 | 分享链接格式：`https://你的域名/?view=令牌` |
| 5.4 | 前端检测 `?view=` 参数 → 进入**访客只读模式**：隐藏编辑/删除/导出/设置入口，只读拉取云端数据渲染所有页面 |
| 5.5 | 令牌校验：无效/过期令牌 → 显示"分享链接无效" |
| 5.6 | 主账号控制：随时换令牌（旧链接立即失效）/ 关闭分享 |

**分享模式安全要点**：
- 令牌为随机长串（如 32 位），不可猜测
- 数据按令牌只读授权（CloudBase 自定义安全规则：`auth 已登录 or 令牌匹配`）
- 访客只读：无法写数据库（安全规则拒绝写操作）
- 不需要访客注册/登录，点链接即看

**关键设计决策**（实施时确认）：
1. **同步策略**：写操作先本地后云端（秒响应），读操作本地优先（离线可用）
2. **冲突处理**：个人单设备为主，采用"最后写入覆盖"，不做复杂合并
3. **离线队列**：断网时写操作暂存队列，恢复网络后批量同步
4. **数据备份**：云端保留每日自动备份（CloudBase 自带），本地导出功能保留
5. **分享权限**：访客只读（不能编辑/删除/导出），主账号可随时收回

---

## 六、技术备注（给非技术背景的 Jeff）

- **CloudBase**：腾讯云的"云开发"服务，一个平台搞定网站托管+数据库+登录，类似"给网页租个云主机+云数据库"。
- **静态托管**：把你的网页文件放到云上，别人通过网址就能访问，不用开自己电脑。
- **DAO 层**：数据存取的"中间人"。现在数据直接存浏览器，接入后端后改成"先存浏览器、再同步云端"，两层都写，断网也能用。
- **匿名登录**：不用注册账号，平台自动给每个访客一个身份，数据自动归属到这个人——对你这样个人用最省事。
- **同步**：手机记一条，电脑自动出现，两边数据一样。
