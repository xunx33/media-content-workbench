# media-content-workbench · 单文件臃肿度评估与拆分规划

> 评估时间：2026-08-05 · 版本：最新 main（b79c125，2943 行）
> 目标：判断单文件是否臃肿、是否需要拆分、怎么拆最安全

---

## 一、现状量化分析

### 1.1 文件构成

| 部分 | 行数 | 占比 | 大小 |
|------|------|------|------|
| CSS 样式 | 856 行 | 29% | ~45 KB |
| HTML 骨架 | 34 行 | 1% | ~2 KB |
| JavaScript 逻辑 | 2042 行 | **69%** | ~91 KB |
| **合计** | **2943 行** | 100% | **138 KB** |

### 1.2 JS 各模块体量

| 模块 | 行数 | 说明 |
|------|------|------|
| 表格解析模块（4 平台列规则） | **623 行** | 最肥的模块 |
| 数据复盘（DATA） | 342 行 | 周/月复盘 + 双子视图 |
| 单条数据录入弹窗 | 160 行 | 挂在登记内容上 |
| CONFIG + STATE | 130 行 | 配置与状态 |
| 今日待办 | 129 行 | 主视图 |
| UTIL + MODAL | 113 行 | 工具 + 弹窗 |
| 示例检测 + INIT + 周提醒 | 113 行 | 启动逻辑 |
| 导出导入 | 104 行 | 备份恢复 |
| 发布总览 | 99 行 | 统计视图 |
| 发布日历 | 97 行 | 日历视图 |
| 流程链路 | 58 行 | 链状态辅助 |
| 统一 CONFIRM + NAV + TOAST | 40 行 | 全局组件 |

### 1.3 结论：是否臃肿？

**判断：需要拆分，但优先级不是"紧急"，而是"现在正是最佳时机"。**

| 维度 | 评分 | 说明 |
|------|------|------|
| 体积 | 🟡 中 | 138KB 对首次加载影响不大（gzip 后约 40KB），尚可接受 |
| 可维护性 | 🔴 差 | 2943 行单文件，改一个模块要翻几百行，容易误伤 |
| 模块耦合 | 🟡 中 | 各 Tab 渲染函数已按注释分区，内部结构清晰，但都挂在全局 |
| 协作性 | 🔴 差 | 多人在一个文件里改代码必然冲突 |
| 未来扩展 | 🔴 差 | 后端接入、PWA、新平台规则会让文件继续膨胀 |

**核心风险点**：表格解析模块 623 行 + 数据复盘 342 行，两个模块占了 JS 近一半。每次加一个平台表格规则，都要在 3000 行文件里精确定位插入点。

---

## 二、拆分方案（推荐：渐进式 CSS/JS 外置，不引框架）

### 2.1 目标结构

```
media-content-workbench/
├── index.html              # 骨架 + 结构（34 行）
├── css/
│   ├── base.css            # 变量/全局/布局/响应式（~200 行）
│   └── components.css      # 卡片/按钮/日历/图表/弹窗等组件（~650 行）
├── js/
│   ├── config.js           # 平台常量 + 配置（130 行）
│   ├── store.js            # 数据读写 + 任务生成/迁移（未来接后端的接口层）
│   ├── ui.js               # UTIL + MODAL + CONFIRM + TOAST + NAV（~170 行）
│   ├── views/
│   │   ├── today.js        # 今日待办 + 流程链路（~190 行）
│   │   ├── calendar.js     # 发布日历（97 行）
│   │   ├── overview.js     # 发布总览（99 行）
│   │   ├── content.js      # 内容登记 + 数据录入弹窗（~195 行）
│   │   ├── table-parser.js # 表格解析（623 行，单独成文件）
│   │   └── data.js         # 数据复盘（342 行）
│   └── app.js              # 入口：加载模块、事件绑定、INIT（~150 行）
└── manifest.json / sw.js   # （PWA 阶段再加）
```

### 2.2 拆分原则

1. **只用原生 ES Module（`<script type="module">`）**，不引打包工具、不引框架——保持零依赖风格
2. **先拆 CSS 和 JS 文件，不改任何业务逻辑**，保证功能 100% 不变
3. **每个文件一个职责**：一个 Tab 一个视图文件
4. **全局状态用单例 store**：`tasks/contents/stats/aiStats` 收进 `store.js`，视图只调用读写接口

### 2.3 加载方式

```html
<link rel="stylesheet" href="css/base.css">
<link rel="stylesheet" href="css/components.css">
<script type="module" src="js/app.js"></script>
```

`app.js` 内部按需 import：

```javascript
import { initStore } from './store.js';
import { renderToday } from './views/today.js';
// ... 各视图导出自己的 render 函数
```

### 2.4 迁移步骤（分 3 步，每步可独立验证）

| 步骤 | 动作 | 验证方式 |
|------|------|----------|
| **Step 1** | 把 856 行 CSS 拆到 `css/base.css` + `css/components.css` | 页面视觉与之前逐像素一致 |
| **Step 2** | 把 JS 按模块拆成上述文件，用 ES Module 组装 | 5 个 Tab 全部功能回归测试通过 |
| **Step 3** | 顺手引入 `store.js` 抽象 + 预留后端接口签名 | 导出/导入、旧数据迁移仍正常 |

### 2.5 风险控制

- **拆完先跑回归**：重点验证 ①数据读写不丢 ②5 个 Tab 渲染正常 ③表格解析能跑 ④导出导入可用
- **ES Module 需 HTTPS 或 localhost**：直接双击打开 file:// 可能因浏览器 CORS 限制失败——部署到 GitHub Pages/Cloudflare Pages 后正常，本地开发用 `python -m http.server` 起服务
- **缓存问题**：拆分后首次加载多几个请求，本地服务器响应快无感；后续上 PWA 时统一缓存策略解决

---

## 三、备选方案对比

| 方案 | 优点 | 缺点 | 建议 |
|------|------|------|------|
| **A. 纯 CSS/JS 外置（推荐）** | 零依赖、改动小、风险低、立即可做 | 文件多了请求数略增 | ✅ 现阶段最佳 |
| B. 引入 Vite + 模块化 | 开发体验好、可上 TS、热更新 | 引入构建链、上手成本、部署配置 | 数据/协作需求爆发后再考虑 |
| C. 引入轻框架（Vue/Lit） | 组件化彻底 | 重写整个渲染层，风险高 | 不推荐现在做 |
| D. 保持单文件 | 零改动 | 继续膨胀，维护越来越难 | 只适合不再迭代的情况 |

---

## 四、执行建议

**推荐方案 A，按 Step 1 → Step 2 → Step 3 推进**，整体预估 1~2 天。

- 如果只想做最小改动：**只拆 JS 表格解析模块（623 行）和 CSS**，其余暂留
- 如果希望一次到位：按完整结构拆，顺带把 `store.js` 数据层抽象做出来，为后续「后端存储接入」铺路——这一步能直接复用，一举两得
- 拆分完成后，再进入 PWA 集成（manifest + sw.js 缓存拆分后的静态资源）

---

## 五、附：为什么现在拆是"最佳时机"

- 功能已进入**相对稳定期**（核心链路、弹窗、搜索刚完成），不会边拆边大改
- 下一步是 PWA + 后端接入，这两个都要求**多文件结构**（manifest、sw.js、API 层），现在拆正好为它们铺路
- 拆完顺手做的 `store.js` 数据抽象，是后端接入的前置条件，避免到时候再大动
